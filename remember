In RDS, RR can be WITHIN-AZ, CROSS-AZ and CROSS-REGION
We don't pay for RR fees if they are in the same region

AWS Identity Center: Multi-account permissions and application assignment

Network Firewall: Activate FLOW INSPECTION to detect intrusion-prone traffic

Field-level encryption is used to securely upload user-submitted data

RDS enchanced monitoring

Usage of AD Federation Service (AD FS) to authenticate users in AWS without creating IAM users


